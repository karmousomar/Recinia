@model MotDePassOublier
@{
    ViewData["Title"] = "Oublier";
}
@using (Html.BeginForm())
{
    <div class="ui raised very padded text container">
        <h2 class="ui header centered">Mot De Pass Oublier !</h2>

        <div class="ui stacked segment">
            <div class="field">
                <div class="ui left icon input">
                    <i class="user icon"></i>
                    @*<input asp-for="Email" placeholder="Email Address" />*@
                    @Html.EditorFor(m => m.Email)
                </div>

            </div>
            <input type="submit" class="ui large grey submit button" name="Done" value="Send" />
            <div asp-validation-summury="All" class="ui error message"></div>
        </div>

    </div>
}